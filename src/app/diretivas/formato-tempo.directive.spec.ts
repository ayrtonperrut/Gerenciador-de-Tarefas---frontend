import { FormatoTempoDirective } from './formato-tempo.directive';

describe('FormatoTempoDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatoTempoDirective();
    expect(directive).toBeTruthy();
  });
});
